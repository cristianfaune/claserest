<nav class="navbar navbar-default navbar-fixed-top">
  <div class="navbar-header">
    <button class="navbar-toggle collapsed" type="button" data-toggle="collapse" data-target="#gatunos">
      <span class="sr-only">Desplegar</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    <% if user_signed_in?%>
    <p>Bienvenido! <%=current_user.name%> <%=image_tag current_user.avatar, class:"img-circle avatar"%></p>
    <% end %>
  </div>
  <div class="collapse navbar-collapse" id="#gatunos">
    <ul class="nav navbar-nav navbar-right">
      <!--agregar palabra path y metodos-->
      <% if user_signed_in?%>
      <li><%= link_to 'Cerrar Sesión', destroy_user_session_path, method: :delete %></li>
      <li><%= link_to 'Editar Perfil', edit_user_registration_path %></li>
      <%else%>
      <li><%= link_to 'Registrarse', new_user_registration_path %></li>
      <li><%= link_to 'Ingresar', new_user_session_path %></li>
      <% end %>
    </ul>
  </div>
</nav>
